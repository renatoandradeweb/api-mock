// src/app/store/app-state.model.ts

export interface AppState {
  clients: any[]; // Utilizando any para representar os objetos cliente diretamente
  // Outras partes do estado podem ser adicionadas conforme necessário
}
